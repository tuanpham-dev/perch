{
  "name": "tasks",
  "private": true,
  "publisher": "perch",
  "version": "1.0.0",
  "displayName": "Tasks",
  "description": "Built-in TASKS run section: package.json scripts from every workspace package, run in reusable named terminal windows with running indicators (bundled — see extensions/build.mjs).",
  "icon": "./icon.svg",
  "perch": {
    "client": "./dist/client.js",
    "server": "./server.js"
  },
  "dependencies": {
    "fast-glob": "^3"
  }
}
